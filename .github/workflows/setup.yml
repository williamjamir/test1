name: Check

on:
  workflow_call:
    
jobs:

  ssome-linters:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Run SETUP
        run: |
          echo "MY SETUP" 