name: Check

on:
  workflow_call:
    
jobs:

  ssome-linters:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run TESTS
        run: |
          echo "MY TESTS" 